# BACKLOG - kernelkit

src/affinity.rs:78 | bug | high | read_irq_affinity miscalculates CPU core indices on systems with more than 32 cores because it assumes each group in the smp_affinity file is 64-bit while the Linux kernel outputs them as comma-separated 32-bit (8 hex digits) groups. | Change the group index multiplier from 64 to 32 and update the bit-shifting loop to iterate 0..32. | status=done note="multiplier fixed to 32, bit loop 0..32, extracted parse_irq_affinity, added unit tests"
src/binformat.rs:371 | perf | medium | The property-based test file_header_write_then_read_is_identity leaks memory because it calls Box::leak on the magic vector on every iteration of proptest without freeing it, leading to unbounded heap growth during test runs. | Pass &magic directly instead of Box::leak since FileHeader does not require a static lifetime in this scope. | status=done note="Box::leak removed, &magic passed directly"
tests/adversarial_impossible.rs:365 | bug | medium | test_impossible_readahead_overflow asserts that readahead::readahead returns an error, but on non-Linux target platforms readahead is a no-op that returns Ok(()), causing the test to fail. | Add a target_os = linux cfg guard to the test, or adjust the assertion to expect Ok(()) on non-Linux. | status=done note="#[cfg(target_os = \"linux\")] guard added"
tests/adversarial_impossible.rs:402 | bug | medium | test_impossible_readahead_negative_offset asserts that readahead::readahead returns an error, but on non-Linux target platforms readahead is a no-op that returns Ok(()), causing the test to fail. | Add a target_os = linux cfg guard to the test, or adjust the assertion to expect Ok(()) on non-Linux. | status=done note="#[cfg(target_os = \"linux\")] guard added"
src/perf.rs:114 | silent-fallback | low | In PerfCounter::read, when libc::read returns 0 (EOF) instead of 8 bytes, the code returns Error::System with last_os_error(). Since read returning 0 does not set errno, this error reports a stale and incorrect OS error code. | Check for n == 0 separately and return a custom error, or map it to a distinct non-OS error. | status=done note="n==0 now returns UnexpectedEof; regression test added via from_raw_fd helper"
tests/test_depth_gap.rs:76 | test-gap | low | test_depth_gap_readahead_io_error contains a meaningless assertion assert!(res.is_ok() || res.is_err()) that is always true, failing to verify the correctness of the returned value of readahead. | Replace the assertion with one checking the expected value (e.g. verifying res.is_ok() on non-Linux). | status=done note="assertion replaced with platform-specific expected result"
tests/test_depth_property.rs:32 | test-gap | low | test_depth_property_readahead contains a meaningless property assertion prop_assert!(res.is_ok() || res.is_err()) that is always true, failing to verify any invariants of readahead. | Replace with a property check verifying that the operation returns Ok(()) on non-Linux platforms, or specific error types on Linux. | status=done note="property assertion replaced with platform-specific expected result"
src/prefetch.rs:3 | bug | high | Compiling for target_arch = x86 (32-bit) fails because it unconditionally attempts to import intrinsics from the core::arch::x86_64 module, which does not exist on 32-bit targets. | Import the intrinsics from core::arch::x86 for target_arch = x86 or restructure the imports conditionally. | status=done note="x86/x86_64 imports gated by cfg(target_arch)"
src/prefetch.rs:51 | bug | high | Compiling for target_arch = x86 (32-bit) fails at compilation because it references core::arch::x86_64::_MM_HINT_NTA directly, which is only defined on x86_64 targets. | Use core::arch::x86::_MM_HINT_NTA on x86 (32-bit) or import _MM_HINT_NTA conditionally to avoid hardcoded x86_64-specific paths. | status=done note="_MM_HINT_NTA selected via cfg(target_arch)"
src/cpu_features.rs:120 | bug | medium | detect_neon on 32-bit ARM target_arch = arm is hardcoded to return true, but NEON is an optional instruction set extension on many older ARMv7 / ARMv6 processors, which will result in SIGILL crashes. | Use std::arch::is_arm_feature_detected!(neon) to perform runtime detection on 32-bit ARM. | status=done note="uses is_arm_feature_detected!(\"neon\") on arm"
src/mmap.rs:206 | bug | high | bind_to_numa_node computes `maxnode = mask_len * size_of::<c_ulong>() * 8 + 1`. mbind's maxnode is a BIT count and the kernel copies BITS_TO_LONGS(maxnode) = mask_len+1 unsigned longs from the nodemask buffer, but `nodemask` (line 204) is only mask_len ulongs -> the kernel reads 8 bytes PAST the Vec allocation (copy_from_user OOB), pulling garbage high-node bits that can spuriously fail mbind (EINVAL) or bind to wrong nodes. Identical bug exists in mempool arena/mmap.rs:208 (already flagged there) - the two are duplicate implementations (see mempool dedup row). VERIFIED manually 2026-07-14. | Set maxnode = mask_len * BITS_PER_ULONG (no +1), or node + 1. Then dedup with mempool per ONE-PLACE. Add a test binding to node 0 that asserts mbind succeeds (or fails only with the expected errno), on a NUMA host. | status=done note="extracted numa_nodemask_for_node helper, maxnode now node+1, exposed shared kernelkit::numa::bind_memory_to_node; mempool now calls it and removed its local mbind copy; unit test verifying mask length and maxnode match added to kernelkit::numa tests"
src/mmap.rs:30 | bug | high | MmapBlock::new and MmapBlock::new_on_node treat MADV_HUGEPAGE rejection as a fatal allocation failure, even though the hint is advisory and can fail for small or unsupported mappings. | Ignore madvise(HUGEPAGE) errors for anonymous MmapBlock allocations; keep the mapping. | status=done note="advise_hugepage errors ignored in MmapBlock constructors"
src/mmap.rs:247 | doc-bug | low | open_read docstring claims it uses MAP_POPULATE to prefault pages, but the implementation maps lazily and no longer sets MAP_POPULATE. | Update docstring to state lazy mapping and the SIGBUS risk. | status=done note="docstring corrected to state lazy mapping and MADV hints"

- [2026-07-14 publish-prep] performance/memory/kernelkit | patch bump 0.1.1->0.1.2 (published max was 0.1.1); no code change; publish-clean (crates.io deps only, faultkit is dev-dep).
src/numa.rs:208-218 | bug | medium | node_count() computes let count = library.max_node().saturating_add(1)... numa_max_node() returns -1 as its ERROR sentinel, but saturating_add(1) turns -1 into 0 before try_from guard runs, so try_from(0)=Ok(0) and unwrap_or(1) is NOT triggered — node_count() returns 0, violating node_count() >= 1 contract. | Check max_node >= 0 before saturating_add and apply count.max(1). | status=done note="max_node checked >= 0 and count.max(1) applied; unit test added"
src/numa.rs:72-101 + :177-198 | one-place,silent-fallback | medium | Claude 2026-07-17: two divergent owners for the same operation "place these pages on NUMA node N". bind_memory_to_node (72) uses the RAW SYS_mbind syscall (MPOL_BIND|MPOL_MF_MOVE), needs no libnuma, and FAILS LOUD (Error::System) if the kernel rejects it. migrate_to_node (177) — the path alloc_on_node uses — instead calls libnuma numa_tonode_memory guarded by `LinuxNuma::load()? && has_multiple_nodes()`, so when libnuma is absent OR unloadable on a genuinely multi-node box it is a SILENT no-op returning Ok(()): the locality the caller requested silently did not happen, with zero operator signal (Law 10). Same conceptual op, two implementations, opposite failure semantics; the raw-mbind path proves libnuma is not even required. | Unify on one mechanism: back migrate_to_node with the raw mbind path (works without libnuma) so alloc_on_node's page placement either succeeds or errors, never silently skips on a multi-node host. If a no-op is ever legitimate (single node), it must be provably single-node, not "libnuma missing". Proving test: on a multi-node host with libnuma unavailable, migrate_to_node returns an error (or performs the mbind), not a silent Ok.
src/numa.rs:399-401 + :421-426 | test-gap | low | Claude 2026-07-17: the numa test module never exercises the two failure modes above. node_count_is_at_least_one only checks the happy path (real machine, max_node>=0), so the -1-sentinel guard-defeat is untested; invalid_node_is_rejected uses node_count() itself to pick an out-of-range node, so it cannot catch node_count()==0. There is also no test that migrate_to_node/alloc_on_node surface (rather than swallow) a missing-libnuma migration on a multi-node system. | Add tests that inject/simulate max_node()==-1 (node_count==1, validate_node(0)==Ok) and that assert the migrate path is loud when libnuma is unavailable, per the two rows above.
